# Dependencies — regenerated during build
node_modules
vendor

# Local build output — rebuilt during Docker build
dist

# Unified Monolith now uses the backend folder in the root Dockerfile!
# Git & IDE
.git
.gitignore
.agent
.worktrees

# Dev/temp files
*.zip
*.lnk
temp_out.txt
knowledge
scripts

# Environment secrets — never ship to container
.env
